services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - yinvdeli_data:/app/data
    environment:
      - DATABASE_PATH=/app/data/inventory.db
    restart: always

volumes:
  yinvdeli_data:
